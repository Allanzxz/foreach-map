/*
Questão 13 - Quando você usaria map() em vez de forEach()?    
*/
//Você usaria map() em vez de forEach() quando precisar transformar os elementos de um array em um novo array com base em alguma lógica aplicada a cada elemento.